apiVersion: v1
kind: Service
metadata:
  name: {{ include "pangea-web.fullname" . }}
  labels:
    {{- include "pangea-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pangea-web.selectorLabels" . | nindent 4 }}
